v26.7までを読んでアドバイスを作りました。
以下のアドバイスに従って、さらなる改良のループで研究を続けてください

---

# 結論

**v26.8には進むべきです。ただし、現行ロードマップの前に `v26.7.1` と `v26.8-0` を挿入すべきです。**

次の中心課題は、いきなり (q^4\log q^2) を測ることではありません。

> **BOND-A の格子 source が、連続極限で 2-taste Dirac の保存された Hilbert/Belinfante stress tensor に流れるかを証明する。**

これに失敗した場合、経路 B は「重力真空偏極」ではなく**格子フェルミオンの弾性・strain response**です。その時点で gravitational interpretation を終了するのが正しいです。

また、v26.7の数値結論自体はおそらく維持されますが、下記二点は downstream の型エラーを防ぐため、先に修正した方がよいです。

---

## 1. 最初に `v26.7.1` として修正すべき二点

### 1.1 massless pole の spectral 変数がずれている

現在の記録は

[
s_n=\Delta E_n^2,
\qquad \mathbf q=2\pi/16\ \text{固定}
]

としながら、pole 条件を (s_n\to0) と書いています。しかし固定非零運動量における massless state は

[
\Delta E_n^2-\mathbf q^2=0
]

に存在します。つまり massless pole の位置は (s=\mathbf q^2) であって (s=0) ではありません。実際、現在のデータ自身が (s_{\min}/q^2\to1.05) と光円錐への収束を示しています。([GitHub][1])

したがって、spectral variable を

[
M_n^2:=\Delta E_n^2-\mathbf q^2
]

に変更し、pole residue を

[
Z_{\rm pole}
============

\lim_{\epsilon\to0^+}
\lim_{L\to\infty}
\sum_{0\le M_n^2<\epsilon}\frac{Z_n}{V}
]

で定義すべきです。有限 (a) では、通常の (q) と tree-level improved momentum

[
\widehat q=\frac{2}{a}\sin\frac{aq}{2}
]

の両方を記録し、両者が同じ continuum limit に行くことを確認します。

現在得られている「最低 light-cone cluster の重みが (N^{-5.4}) 程度で消える」という事実は、変数を直しても**孤立 pole ではなく連続体 threshold である**ことを支持します。したがって主結論の反転ではなく、判定規則の修正になる可能性が高いです。

### 1.2 (T_{yy}) の spectral weight は、そのまま dynamic gauge contamination ではない

静的外場 (q_0=0,\mathbf q\parallel\hat y) に対して (h_{yy}) が純ゲージなのは正しいです。しかし spectral line の行列要素では、各状態について

[
\Delta E_n,\langle0|T_{0\nu}|n\rangle
=====================================

q_i,\langle0|T_{i\nu}|n\rangle
]

という4次元 Ward relation が成立します。したがって (T_{yy}) 単独が大きな spectral weight を持つことは、保存則違反を意味しません。完全な gauge polarization には (T_{00},T_{0i},T_{ij}) の組合せが必要です。

共変 Ward identity は、二次 metric variation、contact term、temporal components を含む全体に対して成立するものです。([arXiv][2])

よって v26.7 の

> 「縦 spectral weight が spin-2 と同程度なので、静的非共変汚染の動的対応物」

という解釈は、

> 「(T_{yy}) spectrum は大きいが、4D temporal block 未実装のため dynamic Ward 診断にはまだ使えない」

へ downgrade するのが正確です。

---

## 2. 現行 `Projector.lean` は4次元 projector ではない

現在の証明対象は明示的に**3次元静的空間計量 (h_{ij})** です。さらに、Lean が直接証明しているのは ({0,\dots,8}^3) 上の恒等式であり、実数全域への拡張は次数勘定による未形式化の補題に依存しています。READMEも certificate を「有限格子＋次数勘定」と記述しています。([GitHub][3])

4次元では transverse subspace の次元が3なので、

[
P^{(0)}*{\mu\nu,\rho\sigma}
=\frac13\theta*{\mu\nu}\theta_{\rho\sigma},
]

[
P^{(2)}_{\mu\nu,\rho\sigma}
===========================

\frac12
(\theta_{\mu\rho}\theta_{\nu\sigma}
+\theta_{\mu\sigma}\theta_{\nu\rho})
-\frac13\theta_{\mu\nu}\theta_{\rho\sigma}.
]

rank はそれぞれ

[
\operatorname{rank}P_2=5,\quad
\operatorname{rank}P_1=3,\quad
\operatorname{rank}P_{0s}=1,\quad
\operatorname{rank}P_{0w}=1
]

です。([arXiv][4])

特に、座標順を ((\tau,x,y,z))、運動量を (q\parallel\hat y) とすると、現行の正規化した

[
S_3=\frac{E_{xx}+E_{zz}}{\sqrt2}
]

は4次元の純 spin-0 ではありません。正しい4次元 scalar と、同じ対称性を持つ spin-2 成分を

[
e_0=\frac{E_{\tau\tau}+E_{xx}+E_{zz}}{\sqrt3},
]

[
e_{2,0}
=\frac{-2E_{\tau\tau}+E_{xx}+E_{zz}}{\sqrt6}
]

とすると、

[
\boxed{
S_3=\sqrt{\frac23},e_0+\frac1{\sqrt3},e_{2,0}
}
]

です。したがって完全共変核が得られた後でのみ、

[
K_{S_3S_3}=\frac23F_0+\frac13F_2,
\qquad
F_0=\frac{3K_{S_3S_3}-F_2}{2}
]

と復元できます。

一方、(D=xx-zz) と (X=xz+zx) は (q_0=0,q\parallel y) でも真の4次元 transverse-traceless 成分です。したがって、

* **v26.8の最小 TT universality test は D/X だけで実施可能**
* **S を4D spin-0と呼ぶのは禁止**
* **full gravitational polarization と呼ぶのは (h_{00},h_{0i}) 実装後**

という線引きが適切です。

---

# 3. 推奨する新しいロードマップ

## v26.8-0 — source matching と定義閉包

ここでは物理値をまだ走らせません。

### 3.1 `ProjectorND.lean` を作る

有限格子の全探索ではなく、

[
\theta^2=\theta,\quad
\omega^2=\omega,\quad
\theta\omega=0,\quad
\theta+\omega=I
]

から projector 代数を抽象的に証明します。

成果物は次です。

* 任意次元 (d) の Barnes–Rivers algebra
* (d=3) と (d=4) の instance
* rank formula
* (P_{0sw},P_{0ws}) を含む scalar block の matrix-unit algebra
* 現行 (S_3) の4D分解
* (D,X) が4D (P_2) 固有ベクトルであること
* (q^2\ne0) 条件を型に含めること

これにより「Lean証明済み」の意味が、有限窓 certificate から全運動量の定理になります。

### 3.2 BOND-A の continuum vertex を導出する

各 Dirac node の近傍で、spin⊗taste 基底へ変換した格子頂点を

[
V_A^{\rm lat}(p,q;a),
\qquad A\in{D,X}
]

とし、次を証明します。

[
\boxed{
Z_A(a)V_A^{\rm lat}(p,q;a)
==========================

I_{\rm taste}\otimes V_A^{\rm Dirac}(p,q)
+O(a^r)
}
]

ここで許される差は、

* EOM operator
* total derivative
* improvement term
* local contact term

だけです。

taste-nonsinglet residual を

[
r_A(a)
======

\frac{
\left|
V_A^{\rm lat}
-I_{\rm taste}\otimes
\frac{\operatorname{tr}_{\rm taste}V_A^{\rm lat}}{2}
\right|
}{
|V_A^{\rm lat}|
}
]

のように定義し、(r_A(a)\to0) を事前登録します。

**この tree-level matching に失敗した場合、one-loop 数値計算へ進んではいけません。** BOND-A は gravitational source ではなく strain source と確定します。

### 3.3 D と X に別々の (Z) を許す

有限格子では diagonal-traceless component と off-diagonal component は異なる格子既約表現に属します。4次元 hypercubic lattice の EMT でも、off-diagonal sextet と diagonal traceless triplet に分かれ、別の renormalization constants が必要です。現在の空間格子でも同型の cubic splitting を想定すべきです。([arXiv][5])

したがって有限 (a) で bare (D=X) を要求してはいけません。

[
Z_D,T_D^{\rm lat},
\qquad
Z_X,T_X^{\rm lat}
]

を、同じ continuum Ward/Feynman–Hellmann 条件で個別に正規化し、その後に

[
F_D^{\rm ren}-F_X^{\rm ren}\to0
]

を universality test とします。

---

## v26.8-A — 解析 one-loop oracle

解析値は二つの独立経路から導出します。

1. Dirac stress vertex を使った直接 Feynman-parameter 積分
2. 曲率 form factor / Weyl-anomaly coefficient からの導出

次の規約を最初に固定します。

* Euclidean/Minkowski signature
* (\Gamma=-\log Z) と (E_0) の符号対応
* (\delta S=\frac12\int h_{\mu\nu}T^{\mu\nu})
* D/X の Frobenius normalization
* 1 Dirac flavor と2 tastes の係数
* Fourier transform と体積規格化
* kernel を (\Gamma^{(2)}=\frac12hKh) と書くかどうか

既知の continuum oracle では、Dirac fermion の Weyl-squared sector は UV で (1/20) に対応する係数を持ち、massive IR では (q^2/m^2) で decouple します。ただし論文ごとに effective-action と kernel の符号・2倍係数が異なるため、現在のコード規約へ写像する前に数値定数を freeze してはいけません。([arXiv][6])

登録すべき主張は数値そのものではなく、

[
\frac{A_{\rm oracle}^{(2\ taste)}}
{2A_{\rm oracle}^{(1\ Dirac)}}=1
]

および二つの解析導出の完全一致です。

---

## v26.8-B — staggered TT continuum limit

### 3.4 (q^4\log q^2) を通常の多項式 fit で取らない

期待される形が

[
K_T(q)
======

c_0+c_2q^2+c_4q^4
+Aq^4\log(q^2/\mu^2)+\cdots
]

なら、4個以上の異なる運動量 (q_i) に対して係数 (w_i) を

[
\sum_iw_i=0,\qquad
\sum_iw_iq_i^2=0,\qquad
\sum_iw_iq_i^4=0,
]

[
\sum_iw_iq_i^4\log q_i^2=1
]

となるように作り、

[
\boxed{
A_{\rm null}(a)=\sum_iw_iK_T(q_i,a)
}
]

を測る方がよいです。

これは (q^0,q^2,q^4) の全 local counterterm を代数的に消します。renormalization point (\mu) への依存も (\sum w_iq_i^4=0) によって消えます。通常の多項式 fit より、はるかに falsifiable です。

さらに spectral discontinuity は local polynomial counterterm で変化しないので、

1. lattice spectral density
2. subtracted dispersion から再構成した Euclidean form factor
3. 直接計算した Euclidean form factorの null projection

の三者一致を要求すべきです。absorptive part は renormalization によって変わらず、polynomial part のみが scheme 依存であることは continuum EMT correlator でも明示されています。([arXiv][7])

### 3.5 continuum trajectory

既存の lattice-unit (m=0.5) を固定したまま (a\to0) にしてはいけません。必要なのは

[
am(a)=a,m_{\rm phys},
\qquad
aq(a)=a,q_{\rm phys},
\qquad
N(a)=L_{\rm phys}/a
]

です。

最低でも次の二系列を分離します。

* **massless / UV系列:** (m=0) または (q^2\gg m^2)
  → (q^4\log q^2) coefficient
* **massive系列:** (q^2/m^2) を広く走査
  → full form factor と IR decoupling

有限質量で (q\ll m) の領域を (q^4\log q^2) だけで fit するのは誤りです。その領域は解析的な decoupling expansion になります。

自由理論なので、主計算は有限 (N^3) 対角化より**無限体積 Brillouin-zone 積分**を優先し、有限体積 engine は独立照合として残すのが効率的です。

---

## v26.8-C — Wilson 独立離散化

Wilson 側は、可能なら current model と同じく**時間連続・空間格子 Hamiltonian**として実装します。これにより temporal regulator の差を混ぜずに、空間 UV discretization だけを交換できます。

重要な taste trap があります。

* 3+1D Hamiltonian staggered fermion は continuum で2 flavor
* 標準的な4次元 Euclidean staggered action は continuum で4 tastes

です。したがって、時間方向まで離散化した標準 staggered determinant に置き換えながら「2 tastes」を維持することはできません。([arXiv][8])

Wilson continuum extrapolationでも、最初から (O(a^2)) と決め打ちせず、

[
A(a)=A_0+c_1a+c_2a^2
]

と

[
A(a)=A_0+c_2a^2+c_4a^4
]

の両モデルを事前登録し、fit-window variation を系統誤差に含めるべきです。データを見て都合のよい次数を選ぶことは禁止します。

---

## v26.9 — dynamic metric ではなく、4D covariance closure

現行の v26.9 dynamic metric fork は v27.0 へ延期するのが妥当です。

v26.9で実装すべきものは、

* (h_{00})
* (h_{0i})
* 非零 external frequency (q_0)
* full (10\times10) symmetric-tensor kernel
* 4D Ward identity
  [
  q_\mu\Pi^{\mu\nu,\rho\sigma}=0
  ]
* contact/tadpole を含む second metric variation
* spin-0 / spin-2 form factor の完全分離

です。

ここを通過して初めて `FullGravitationalVacuumPolarization` 型を発行できます。それまでは型名を

```text
StaticSpatialStressResponse
TreeLevelMatchedTTSource
RenormalizedTTFormFactor
RegulatorUniversalTT
```

に留めるべきです。

---

# 4. 非常に良い追加 falsifier：Dirac spin-0 spectral sum rule

2026年7月20日に提出された新しい gravitational vacuum polarization の preprint には、4次元 Dirac fermion の有限な spin-0 spectral function が掲載されています。少なくとも現時点では、独立に再導出すべき新しい oracle として扱うのがよいです。([arXiv][2])

1 Dirac fermion について、

[
\sigma_f(s)
===========

\frac{m^2}{24\pi^2s^2}
\left(1-\frac{4m^2}{s}\right)^{3/2}
\Theta(s-4m^2),
]

[
\int_0^\infty ds,\sigma_f(s)
============================

\frac{1}{240\pi^2}.
]

したがって2 tastes なら、

[
\boxed{
\int_0^\infty ds,\sigma_{2t}(s)
===============================

\frac{1}{120\pi^2}
}
]

が regulator-independent target になります。([arXiv][4])

これは (q^4\log q^2) よりも優れた点があります。

* UV finite
* local counterterm に依存しない
* threshold exponent まで比較可能
* mass を変えても積分値が一定
* staggered/Wilson の taste normalization を直接検査できる

完全な gravitational spin-0 claim には4D source が必要ですが、(m\bar\psi\psi) の taste-singlet scalar operator を使えば、先に**spectral-engine の副認証**として実装できます。ただしその段階では gravitational response ではなく operator benchmark と分類します。

また (m\to0) で (\sigma_f(s)\to{\rm const}\cdot\delta(s)) となっても、それを直ちに「4D二点関数に massless graviton/scalar pole が出た」と表現してはいけません。同論文自身、4D二点関数には pole residue が残る一方、完全な anomaly pole は三点関数で初めて現れると区別しています。([arXiv][4])

---

# 5. 事前登録する予言

| ID       | 予言                   | 成功条件                                                                                         | 失敗時の意味                                        |
| -------- | -------------------- | -------------------------------------------------------------------------------------------- | --------------------------------------------- |
| PRED-014 | invariant pole audit | (M^2=\Delta E^2-q^2) の threshold weight が (\epsilon\to0) で0、isolated residueなし               | v26.7 no-pole 結論の再検討                          |
| PRED-015 | source matching      | D/X の taste-nonsinglet residual が (a\to0) で0、finest latticeで (<10^{-3})                      | BOND-A は gravitational source ではない            |
| PRED-016 | TT universality      | staggered D/X、Wilson D/X の全てが2-taste analytic coefficient と1%以内、continuum uncertainty 0.5%以下 | source・taste・renormalization・scaling のいずれかが誤り |
| PRED-017 | spin-0 sum rule      | continuum extrapolated integral が (1/(120\pi^2)) と1%以内                                       | scalar normalization または taste count が誤り      |
| PRED-018 | massive decoupling   | (q^2\ll m^2) で nonlocal running が (q^2/m^2) に比例                                              | full form factor または continuum trajectory が誤り |

PRED-016では bare 値の一致を要求せず、

[
\frac{A_D^{\rm stag}}{A_{\rm oracle}},
\quad
\frac{A_X^{\rm stag}}{A_{\rm oracle}},
\quad
\frac{A_D^{\rm Wilson}}{A_{\rm oracle}},
\quad
\frac{A_X^{\rm Wilson}}{A_{\rm oracle}}
]

の continuum limit がすべて1になることを要求します。

---

# 6. 厳密な終了・分岐条件

### Gate 0: v26.7意味論修正

spectral invariant と longitudinal-spectrum interpretation を修正する。

### Gate 1: tree-level source matching

失敗したら経路 B の gravitational interpretation を終了する。one-loop の一致・不一致を調べても意味がありません。

### Gate 2: continuum oracle の二重導出

直接 loop と curvature/anomaly route が一致しなければ数値実装禁止。

### Gate 3: staggered TT

D/X が個別正規化後に同じ continuum form factor へ行くこと。

### Gate 4: Wilson universality

Wilson と staggered が同じ continuum form factor と spectral discontinuity へ行くこと。

### Gate 5: full 4D Ward

(h_{00},h_{0i}) を含めた4D kernel が Ward identity を満たすこと。

**Gate 5を通るまで (1/\Pi)、graviton propagator、dynamic metric へ進まない**という既存原則はそのまま維持すべきです。

結果の意味も明確です。

* staggered と Wilson が両方 analytic Dirac oracle に一致
  → **測定器が正しい。QRNや創発重力の証拠ではない**
* 両 regulator が互いに一致するが oracle と不一致
  → 共通の source/convention/taste error
* staggered のみ一致
  → Wilson source または continuum fit の失敗
* TT は一致するが full Ward が失敗
  → BOND-A は TT operator としては正しいが、metric source として不完全
* 全部通過
  → 初めて「matter loop による covariant metric kinetic term」の研究資格が生じる

---

# 7. 最も合理的な次セッションの作業単位

次セッションは、物理 run を始めず、以下だけを1つの green commit 群として完了させるのが最善です。

> **PROMPT/9 — v26.7 spectral semantics correction + v26.8 source-matching closure**
>
> 1. (M^2=\Delta E^2-q^2) による pole criterion を再登録し、既存データを再採点する。
> 2. (T_{yy}) spectral weight を dynamic Ward violation とする主張を downgrade する。
> 3. `ProjectorND.lean` / `Projector4D.lean` を作り、有限格子ではなく抽象 projector algebra を証明する。
> 4. (S_3=\sqrt{2/3},e_0+e_{2,0}/\sqrt3) を定理化する。
> 5. BOND-A の D/X 一次頂点を spin⊗taste 基底へ展開し、continuum Dirac TT vertex との matching theorem/certificate を作る。
> 6. matching に成功した場合だけ、v26.8 analytic one-loop oracle の導出へ進む。

この順序なら、最も安価な段階で経路 B を殺せます。逆にここを通過すれば、v26.8 の staggered–Wilson universality audit はかなり強い、独立した論文単位になります。なお、その論文の正確な主題は「誘導重力の発見」ではなく、**機械証明・source matching・二離散化を備えた Dirac stress-tensor vacuum-polarization audit**です。

v25.2 release/Zenodo と anomaly-search / modular-BW 投稿はこの系列と直交しているため、v26.8を待たずに進めるべきです。現時点で最大の不足は内部計算量ではなく、外部に固定された再現単位と独立査読です。

[1]: https://github.com/Yasuo-Higano/Quantum-Relational-Network/commit/84f48e9 "https://github.com/Yasuo-Higano/Quantum-Relational-Network/commit/84f48e9"
[2]: https://arxiv.org/abs/2607.18180 "https://arxiv.org/abs/2607.18180"
[3]: https://raw.githubusercontent.com/Yasuo-Higano/Quantum-Relational-Network/main/proofs/Projector.lean "https://raw.githubusercontent.com/Yasuo-Higano/Quantum-Relational-Network/main/proofs/Projector.lean"
[4]: https://arxiv.org/pdf/2607.18180 "https://arxiv.org/pdf/2607.18180"
[5]: https://arxiv.org/html/2606.28035v1 "https://arxiv.org/html/2606.28035v1"
[6]: https://arxiv.org/pdf/hep-ph/0303124 "https://arxiv.org/pdf/hep-ph/0303124"
[7]: https://arxiv.org/html/2404.04021v1 "Vacuum correlations of the stress-energy-momentum tensor with constituent quarks"
[8]: https://arxiv.org/pdf/2604.02078 "https://arxiv.org/pdf/2604.02078"
