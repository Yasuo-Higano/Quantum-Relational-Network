# 量子情報網理論 v25.2 — λ の解析的完結と v25 系列の凍結

**第二十六期第 2 版 (凍結版)。PROMPT/7 の方針に基づき、本版で v25 系列を閉じる。**
v25.1 で λ の起源は 1D 質量関数 g(μ), g_m(μ) の BZ 平均に厳密還元された。本版は
(I) g の解析形を**既知厳密解の同定**として確定し [exact-g]、(II) BZ moment を区間演算の
証明付き数値にし、異方性を定理化し [bz-certificate]、(III) 有限鎖計算を厳密式への
収束試験として再定義してゲートを fail-closed 化し [finite-size-and-gates]、
(IV) 系列を凍結する [release]。**v25.3 は開始しない** — 外部査読可能な固定物を作る。

## 1. g(μ) の厳密閉形式 — 既知厳密解の同定 (コミット I)

### 1.1 先行研究 (C0)

V. Eisler, "On the Bisognano-Wichmann entanglement Hamiltonian of nonrelativistic
fermions", **J. Stat. Mech. (2025) 013101** [arXiv:2410.16433] は、半無限 staggered 鎖

> Ĥ = −(1/2)Σ(c†ₙcₙ₊₁ + h.c.) + μΣ(−1)ⁿ c†ₙcₙ (同論文 式 (64))

の半無限領域 entanglement Hamiltonian を、相関行列と可換な三重対角行列
T (t_m = −m/2, d_m = (−1)^m μ(m−1/2), 式 (67)) を使い

> **H_EH = 4κ K(κ′) · T**, κ = 1/√(1+μ²), κ′ = |μ|/√(1+μ²) (式 (72))

と**厳密に**与えた (K は第一種完全楕円積分)。結論は「BW 形は gapped 鎖で、分散依存の
非普遍 prefactor を除いて厳密」— v24.4/v24.5 で QRN が実測した「BW 線形**形**は 5 桁で
生存、規格化 λ だけが regulator 物性として走る」の 1D 版と完全に同じ構図である。

本プロジェクトの鎖 (v251 の chain_k, hopping +1/2) とは局所ユニタリ cₙ → (−1)ⁿcₙ で
同値 (staggered 項は不変、ボンド符号のみ反転 — K の勾配絶対値は不変)。

### 1.2 QRN 規格化への翻訳 — g_m ≡ g は定理になる

v25.1 の定義 K_NN = g·πξ + b、(−1)^x K_xx = g_m·2πξ_s·μ + b_m に H_EH = 4κK(κ′)T を
代入すると πg = 2κK(κ′)、2π g_m = 4κK(κ′)。すなわち

> **g(μ) = g_m(μ) = (2/π) κ K(κ′)** — 運動項と質量項の規格化は同一関数 (厳密)。

v25.1 の観測「g_m ≈ g (4–5 桁)」は近似的一致ではなく厳密な同一性だった。
b ≈ 0 (純線形) も Eisler の H_EH に定数項が無いことの帰結。

### 1.3 三表示と漸近形

K(k) = π/(2 AGM(1, √(1−k²))) と AGM の同次性 AGM(ca,cb) = c·AGM(a,b) から:

| 表示 | 式 |
|---|---|
| 楕円 | g = (2/π)·K(κ′)/√(1+μ²) |
| AGM | g = 1/AGM(1, √(1+μ²)) |
| 積分 | g = (2/π)∫₀^{π/2} dθ/√(1+μ²cos²θ) (Legendre 標準形 a=√(1+μ²), b=1) |

漸近形 (v25.1 §4 の開問への解答):

- **小 μ**: g = 1 − μ²/4 + 9μ⁴/64 − 25μ⁶/256 + 1225μ⁸/16384 − … 係数は
  (−1)ⁿ[(2n−1)!!/(2n)!!]²。**μ² の解析級数 (収束半径 |μ|=1) であり、v25.1 が候補に
  挙げた μ²lnμ 型の非解析項は存在しない**。
- **大 μ**: g = (2/(π|μ|))·ln(4|μ|)·(1+O(1/μ²)) — **1/μ² 型でも純 1/μ 型でもなく、
  log 補正つき 1/μ**。
- 分散 E(q) = √(cos²q+μ²) の複素零点から ξ_corr = 1/arsinh|μ|。μ=10⁻³ では
  ξ_corr ~ 10³ ≫ N=256 — v25.1 の小 μ 域が窓に乗らなかった理由も閉じた
  (有限鎖の役割の再定義はコミット III)。

参照値 (15 桁, results/v252_exact_g.txt):

| μ | K(κ′) | g(μ) = g_m(μ) | v25.1 実測 ĝ | 相対差 |
|---|---|---|---|---|
| 0.5 | 1.659623598610528 | **0.945006330929758** | 0.944999 | 7.4e-6 |
| 1 | 1.854074677301372 | **0.834626841674073** | 0.834625 | 2.4e-6 |
| √2 | 2.028959102748815 | **0.745749187316330** | 0.745745 | 5.5e-6 |

g(1) は**ガウス定数** 1/AGM(1,√2) そのもの (外部照合 1 ulp — OEIS A014549)。

### 1.4 検証 (v252_exact_g, 21 検査 PASS / 0 FAIL, 判定 a)

- [S1] 三表示の相互一致: max 相対差 3.4e-14 (μ ∈ [1e-4, 1e4] の 36 点; μ>100 は
  Legendre 形 1−κ′²sin²θ の桁落ちで 2.5e-10 — 許容域を分けて判定)。
- [S2] 小 μ 級数: 残差 g−S₄ = c₅μ¹⁰(1−(121/144)μ²) の**両側**一致 5.4e-3、
  残差スロープ 9.943 (期待 10−O(μ²))。
- [S3] 旧仮説の棄却: (g−1)/μ² = −0.25 ± 1.5e-9 (μ²lnμ 係数 |a| < 5e-6)、
  πμg/(2ln(4√(1+μ²))) = 1−2.8e-7 @μ=10³、μg の log 成長比 1.27762 = ln 比 (5 桁)。
- [S4] **事前登録判定** (設計ノートの規約どおり μ ≥ 0.5 の 48 点):
  max|ĝ/g_exact − 1| = **1.18e-5** (< 5e-5)。全域 max 9.8e-4 は小 μ crossover
  (登録済みの除外域)。
- [S5] g_m ≡ g の実測照合: max|ĝ_m/g_exact − 1| = **4.4e-7** (μ ≥ 0.5) — 質量チャネル
  対角は半サイト格子 ξ_s = ξ−1/2 のおかげでボンドより系統が小さい。
- [S6] 性質: g(0)=1 (bit 一致)・偶関数・0<g≤1・狭義単調減少・
  g′(μ) = −μ⟨cos²θ(1+μ²cos²θ)^{−3/2}⟩ < 0 (数値微分と 1.5e-10 一致 — §4 の
  異方性定理で使う単調性の器械照合)。
- [S7] **変異検出層 (破壊層)**: κ↔κ′ 交換 (32%)・規格 4/π (100%)・母数混同 K(κ′²)
  (11%)・√忘れ AGM(1,1+μ²) (28%) — 全てゲート 1e-3 が検出。「二実装が一致した」
  だけでなく「どの誤りを入れるとどのゲートが反応するか」を証拠化した。

### 1.5 事前登録スクリーニングの決算

設計ノートの粗スクリーニング最良候補 **π/(2K(κ′)) は棄却が確定** (S7-m5 がドリフト
0.16% @μ=0.5 → 3.81% @√2 を再現 — ノートの記録 0.16%→3.8% と一致)。この候補が
「近いのに合わない」理由も構造的に判明した: Eisler (72) より EH スペクトル間隔は
2π K(κ′)/K(κ) = [prefactor 4κK(κ′)] × [T の固有値間隔 π/(2κK(κ))] と**因子分解**され、
スクリーニング候補は後者 (CTM 間隔関数) を測っていた。ボンド勾配 g は前者の
prefactor である。「楕円積分構造 (XY 対応 m = 1/(1+μ²)) の見込み」は的中、
関数の取り違えだけが敗因 — 数秘術ではなく構造推定が正しかった事例として記録。

### 1.6 新規性境界 (C0 / C1 の区別 — 論文切り出しの前提)

- **既知 (C0)**: 1D staggered 鎖の H_EH = 4κK(κ′)T (Eisler 2024/2025)。
  **g(μ) の閉形式の「発見」を本プロジェクトの新規性として主張してはならない**。
- **本版の寄与 (C1)**: (i) v25.1 実測 g, g_m との同定 (事前登録判定 a)、
  (ii) 旧仮説 (μ²lnμ / 1/μ²) と旧候補 (間隔関数) の棄却、(iii) 3+1D staggered の
  λ_x, λ_⊥ を既知 1D 厳密解の BZ moment へ接続する橋 (コミット II で完結)。

## 2. BZ moment の区間証明書と異方性定理 (コミット II)

v25.1 + §1 の帰結として 1/λ_x = r_x = ⟨g(μ)⟩_BZ、1/λ_⊥ = r_⊥ = ⟨2cos²ky·g(μ)⟩_BZ
(μ² = cos²ky + cos²kz) は**引数不要の確定した数**になった。本コミットは「倍精度で
合った」から一段上げ、証明付き数値にする (v252_bz_certificate, 15 検査 PASS)。

### 2.1 証明付き数値

共通の離散化・補間表を共有しない 3 実装:

- **A** (3D トーラス中点則, AGM 不使用): g の積分表示を展開した単一被積分関数
  [1+(cos²ky+cos²kz)cos²θ]^{−1/2} の 3D 平均。n=64 で既に 15 桁収束 (解析的周期関数の
  中点則は幾何収束)。
- **B** (2D BZ 中点則 + AGM): θ 方向を AGM で厳密に畳む。
- **R** (区間 Riemann, n = 2¹⁵): 外向き丸めによる**厳密包含**。

| 量 | 浮動小数 (A = B, 相互差 3.9e-16) | 証明付き区間 (R) |
|---|---|---|
| r_x | 0.843549215293854 | [0.843545503984956, 0.843552926638375] |
| r_⊥ | 0.813385882130202 | [0.813356957837832, 0.813414806840221] |
| **λ_x** | **1.185467287349258** | [1.185462071698428, 1.185472502995920] |
| **λ_⊥** | **1.229428764341310** | [1.229385046338885, 1.229472484821826] |

v24.3 の 3D 直接測定 (λ_x = 1.185468 / λ_⊥ = 1.229430) と**相対差 6.0e-7 / 1.0e-6**
— **v25.0 の残高 1「λ_x, λ_⊥ の解析的導出」は完済**。

### 2.2 異方性定理 (λ_⊥ > λ_x の向きは必然)

**定理**: r_⊥ < r_x (すなわち λ_⊥ > λ_x)。

**証明**: BZ 一様測度で Y = cos²ky と Z = cos²kz は独立、E[Y] = 1/2。
h(s) = g(√s) は狭義単調減少 (g′(μ) = −μ⟨cos²θ(1+μ²cos²θ)^{−3/2}⟩ < 0 — §1 S6e で
器械照合済み)。r_⊥ − r_x = ⟨2Y·h(Y+Z)⟩ − ⟨h(Y+Z)⟩ = 2Cov(Y, h(Y+Z))。Z で条件付ける
と E[Y|Z] = E[Y] (独立) より Cov(Y, h(Y+Z)) = E_Z[Cov(Y, h(Y+Z) | Z)]。各 z について
h(Y+z) は Y の狭義減少関数で Y は非退化だから Chebyshev の相関不等式より
Cov(Y, h(Y+z)) < 0。ゆえに r_⊥ − r_x < 0。∎

区間演算はこれを数値的にも**証明**する: r_x − r_⊥ ≥ 0.0301307 (> 0 厳密)、
**λ_⊥ − λ_x ≥ 0.04391**。

**物理的含意**: 異方性 λ_⊥/λ_x = 1.037084… は Lorentz 破れではなく「横チャネルの
重み 2cos²ky と質量依存 prefactor g(μ) の**負の共分散**」— 重みが大きい (cos²ky 大)
ところほど μ が大きく g が小さい — の幾何学的必然。v25.1 の「チャネル重みの幾何」が
向きの符号まで定理に昇格した。

### 2.3 区間演算の設計 (iv.rs — 仮定 ASM-IEEE754 を新規登録)

外向き丸め (1 ulp ナッジ、IEEE-754 の正しい丸めを仮定)・π は区間定数
[PI, next_up(PI)]・cos は剰余項パディングつき Taylor (K=13, 係数 1/(2k)! も区間) —
**libm を信頼しない**。セルの Y = cos²k は cos の単調性で厳密範囲化し、区間 AGM
(任意ステップで hull ⊇ AGM) → 階層和 (行 → 添字順畳み込み — 平坦和は部分和の桁上がり
でナッジが肥大する) → n² = 2³⁰ の 2 冪除算 (正確)。幅は O(h) (モノトーン包 Riemann の
宿命): r_x 7.4e-6 / r_⊥ 5.8e-5。**「証明は区間 (5–6 桁)、15 桁は独立 2 実装の相互照合」
という分業**を明示する。変異検出層: 重み sin² (逸脱 0.060)・係数 2 忘れ (0.407)・
μ² の kz 落とし (0.055) — 全て検出。

### 2.4 証明書

`results/v252_bz_certificate.json` — 式 (spec 文字列 + SHA-256)・領域・規格化・方法・
区間・ソース (自身と iv.rs) の SHA-256。**決定性のため日付・commit hash は含めない**
(スイートの引用機構が byte 同一性を前提とするため) — 出所同定は凍結マニフェスト (IV)
が担う。

## 3. 有限サイズ収束試験とゲートの fail-closed 化 (コミット III)

### 3.1 v251 の遡及修正 (開発記録)

PROMPT/7 が指摘した 3 つのゲート欠陥を修正した (v25.1 の modular_k 修正と同じ
遡及修正プロトコル — 修正・再走・不変性確認):

1. **fail-open ゲート**: 旧 `xi_hi = xi_trust.max(4)` は信頼域が不成立でも ξ≤4 を
   強制フィットしていた → 窓 [3, ξ*] に最低 3 点 (ξ* ≥ 5) を要求し、不成立は
   None (除外) を返す fail-closed に変更。
2. **到達不能な判定分岐**: 旧 `s3_ok = nfail == 0` は全検査後の評価のため分岐 (b)
   (和則成立・再構成不一致) が構造的に到達不能だった → 段階別 (S0–S4) の失敗計数に分離。
3. **linfit の入力検証なし**: 点数不足・非有限値・分散ゼロで NaN を黙って返し得た →
   lib に `linfit_checked` を追加し経由。

**再走の不変性**: g/g_m/λ 再構成の全公表数値は 15 桁で不変 (linfit の正規方程式化に
よる最終桁 1 ulp 差のみ)。fail-closed 化での除外はゼロ ([S0c] として常設検査化)。
副産物の記載訂正: v25.1 文書の「μ 格子 78 点」は実際には **75 点** (計数ミス — 数値
には無関係)。chain_f/chain_k は stag.rs へ昇格 (v251/v252 で共有)。

### 3.2 役割の再定義 — 収束試験としての有限鎖 (v252_finite_size, 5 検査 PASS)

「フィットから g の形を発見する」から「**厳密な g(μ) への収束を測定する**」への
再定義。これでフィット不一致が (i) 物理式の反証か (ii) 有限サイズ/器械の限界かを
分離できる。結果:

- **器械精度**: max|ĝ/g_exact − 1| = 8.1e-6、max|ĝ_m/g_exact − 1| = **5.3e-7**
  (μ ∈ {0.5, 1, √2} × N ∈ {64, 128, 256, 512} の全 12 点)。質量チャネルは半サイト
  格子と点数の利で 15 倍精度が良い — **Eisler 同定の最強の実測照合**。
- **収束の形は (a-ii) 器械床支配**: 残差は N = 64 → 512 で不変 (収束次数 p ≈ 0)。
  すなわち残差は有限サイズ効果ではなく**窓/クランプの器械床** (ξ* = 5–6 の κ 予算端、
  N 非依存)。v25.1 の抽出精度は「サイズ律速」ではなく「窓律速」だったことが確定 —
  床を下げるには N を増やしても無駄で、qd (四倍精度, κ 床 147 → ξ* ≲ 15) が要る。
- **crossover 則の定量化**: 無次元変数 x = N·arsinh(μ) (ξ_corr = 1/arsinh μ) が
  判定変数。x ≥ 25 → 収束域 (実測 |d| ≤ 6e-6 < ゲート 1e-4)、x ≤ 2 → 鎖は実質
  massless で ĝ は ĝ_N(0) を読む (実測 ≤ 7.1e-4)。**v25.1 の「μ ≥ 0.5 で判定」という
  事前登録除外が、経験則から定量規則に昇格した**。
- **fail-closed 負制御**: 粗クランプ対 (1e-8, 1e-6) は κ 予算 (8ξ+12) が ξ=1 から
  破綻し ξ* = 0 → None が返ることを確認。旧実装なら ξ≤4 の強制フィット値が黙って
  出ていた — 修正の検出可能性を証拠化。
- **一次データ同一性**: N=256 の ĝ, ĝ_m は v251 アーカイブと max|Δ| = 0.0 (bit 同値)。
- 開発記録 (run1 → run2): 初版 S1 ゲートは「err が N で減少する」という**物理仮説**を
  課して FAIL した — 実測が平坦だったため。ゲートは器械精度 (実測に貼る) に移し、
  収束の形は事前登録分岐 (a-i)/(a-ii) の記録に変更 (v24.3 W1/W2 の教訓と同型)。
  run1/run2 で測定値は同一 (失敗走行も results に保存)。

## 4. 凍結 (コミット IV)

### 4.1 凍結宣言

**v25 系列は本版で停止する。v25.3 は開始しない。** moving target のままでは外部再現の
途中で主張が更新され続ける — 本版は「外部査読可能な固定物」を目的とする (PROMPT/7)。
再開条件は反証条件台帳の発火のみ: FAL-SUITE (スイート赤)・FAL-CONTINUUM (連続極限で
の破れ)・または凍結マニフェストの不変性違反。λ 系列の物理的拡張 (長距離チャネル和則・
3+1D Wilson 対照・qd による床下げ) は将来の**新系列**として、凍結物を入力に行う。

### 4.2 凍結マニフェスト (v252_manifest — 常設の不変性監査)

凍結成果物 (文書・論文原稿・4 バイナリのソース・iv.rs・決定的な結果成果物) の
SHA-256 台帳を `results/v252_manifest.json` に固定し、`v252_manifest` が**再走のたびに
照合**する — 凍結の破れはスイートの FAIL として現れる。**タイミング行を含む結果 txt
(v252_bz_certificate.txt / v252_finite_size.txt / v251_gmu.txt) は台帳の対象外**
(スイート再走の byte 決定性がないため) — それらの数値内容は決定的な JSON
(v251_gmu.json / v252_bz_certificate.json) と本文書が固定する。凍結値
(λ の 15 桁・区間・異方性下界) は台帳自身にも埋め込み、証明書 JSON と毎走行照合する。

### 4.3 PASS の分類 (852 個の PASS は等価ではない)

v25.2 の 45 検査 (v252_exact_g 21 / v252_bz_certificate 11 / v252_finite_size 5 /
v251_gmu 改 8) の認識論的内訳:

| 分類 | 個数 | 例 |
|---|---|---|
| 数学的恒等式・厳密値照合 | 14 | 三表示一致、ガウス定数 1 ulp、級数/漸近、g′<0 |
| 既知データとの照合 (内部) | 8 | v25.1 一次データ、v24.3 直接測定、アーカイブ同一性 |
| 内部整合 (独立実装・区間包含・収束) | 8 | A=B (3.9e-16)、R∋A,B、倍増収束、和則 |
| **負制御 (変異・劣化・除外)** | 10 | 式変異 5 種、測度変異 3 種、粗クランプ→None |
| 器械自己検証 | 5 | iv/dd/stag self-test、crossover 較正 |
| **凍結新規予言** | **0** | — 本版は規格化の解析的完結であり新データを予言しない |
| **独立外部再現** | **0** | — 第三者による仕様書ベースの再実装は未実施 (公募事項) |

最後の 2 行が 0 であることを明示するのが本分類の目的である: 既知結果の再現 100 個と
凍結予言の的中 1 個は認識論的に同じ「1 PASS」ではない (PROMPT/7 §8)。

### 4.4 論文切り出しと新規性境界 (再掲)

`paper/modular-bw-full.md` (英語原稿, 対象誌 J. Stat. Mech. / PRB):
**"Exact Brillouin-Zone Moment Formulas for the Lattice Bisognano–Wichmann
Normalization of Three-Dimensional Staggered Fermions"** — タイトルに QRN・量子重力を
入れない (結果単体で査読に耐えさせる)。主張してよいこと / いけないことの境界は
論文 §7 に明文化 (1D 閉形式は Eisler の既知結果、寄与は還元・moment 公式・異方性定理・
境界の機械検証)。「新しい温度の発見」「32/27」「Newton 定数の導出」は**全て否定側の
言明として**論文に含める。

### 4.5 外部手順 (リポジトリ外の作業)

- git tag `v25.2-freeze` を起点に GitHub Release → Zenodo で DOI 発行 (要ウェブ操作 —
  ユーザー作業)。CITATION.cff は既存。
- 独立第三者再現の公募: 仕様 (§1 の式・certificate JSON) だけを渡した再実装。

### 4.6 次期の地図 (凍結後 — PROMPT/7 の残り順序)

1. anomaly-search 論文の主張域限定 (タイトル・定理仮定の明示) — 最初の外部査読単位
2. flavor 三部作 (geometric-yukawa / cp-complex-structure / measure-dissolution) を
   「falsification-driven development」1 本に統合 (prequential 評価)
3. 誘導重力は経路 B (背景計量 → Γ[g], stress tensor 2 点関数) — 面積則係数の再解釈は
   しない (v24.6 の正名を維持)
4. **QRN-Core v1 (単一作用・明示 Hilbert 空間・同一極限から物質と幾何) が定義される
   まで、新しい広域 QRN モジュールの追加は停止**

## 5. 成果物 (凍結版)

- コード: `sim/src/lib.rs` (agm/ellip_k/g_exact/linfit_checked), `sim/src/iv.rs` (区間演算),
  `sim/src/stag.rs` (chain_f/chain_k 昇格), `sim/src/bin/v252_exact_g.rs`,
  `v252_bz_certificate.rs`, `v252_finite_size.rs`, `v252_manifest.rs`, `v251_gmu.rs` (遡及修正)
- 結果: `results/v252_exact_g.txt` (21 PASS), `v252_bz_certificate.txt` (11 PASS) +
  `v252_bz_certificate.json` (証明書), `v252_finite_size.txt` (5 PASS),
  `v251_gmu.txt/json` (8 PASS, 再走不変), `v252_manifest.txt/json` (凍結台帳)
- 文書・論文: 本文書 / `paper/modular-bw-full.md`
- 台帳: QRN-C0-007, QRN-GRAV-031..034, QRN-META-027 / ASM-IEEE754 (新規仮定)
